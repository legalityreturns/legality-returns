import React from "react";
import ReactDOM from "react-dom/client";
import LegalityReturnsApp from "./LegalityReturnsApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LegalityReturnsApp />
  </React.StrictMode>
);